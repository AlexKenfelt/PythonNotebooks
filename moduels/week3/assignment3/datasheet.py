# In DataSheet create a method to get_grades_as_list()

import course as c


class Datasheet () :
    
    """courses = c.Course
    list_of_courses = [courses.name, courses.classroom, courses.teacher]

    test1 = new (list_of_courses = ['English', 'B-225', 'Hans Hansen', 10])
    test2 = courses['English', 'B-225', 'Hans Hansen', 10]
    print(test1)"""

    def __init__(self, corses):
        self.courses = courses [courses.grade]
    
    
    def get_grades_as_list(self):
        grade_list = self.course.grade
        return grade_list

courses = c.Course
class_instance = Datasheet(courses)
class_instance.get_grades_as_list()
